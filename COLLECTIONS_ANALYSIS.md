# Анализ коллекций Directus и передачи данных на фронт

## 📊 Сводная таблица

| Коллекция | Статус | Элементов | Полей запрашивается | Полей передается | ISR кэш |
|-----------|--------|-----------|-------------------|-----------------|---------|
| **hero** | ✅ | 1 | 6 | 6 | 3600с |
| **products** | ✅ | 7 | 8 | 8 | 3600с |
| **news** | ✅ | 6 | 7 | 7 | 60с |
| **menu** | ✅ | ? | 4 | 4 | 3600с |
| **site_settings** | ✅ | 1 | все | 22 | 3600с |

---

## 📋 КОЛЛЕКЦИЯ: HERO

**Тип:** Singleton (1 запись)

**Запрашиваемые поля из Directus:**
```
id, title, subtitle, button_text, button_link, hero_photo.id, hero_photo.filename_disk
```

**Передаваемые поля на фронт (6):**
- ✅ `id` - ID записи
- ✅ `title` - Заголовок "Безглютеновый хлеб и десерты в Москве!"
- ✅ `subtitle` - Подзаголовок
- ✅ `button_text` - Текст кнопки (сейчас NULL)
- ✅ `button_link` - Ссылка кнопки (сейчас NULL)
- ✅ `hero_photo` - Объект с изображением (содержит `id`, `filename_disk`, `url`)

**Потенциально НЕ передаваемые поля (требуют проверки в Directus):**
- ? `image_alt` - Альтернативный текст для изображения (используется в коде, но не запрашивается)
- ? `background_image` - Фоновое изображение (используется в коде, но не запрашивается)
- ? `background_image_alt` - Alt для фонового изображения

**API маршрут:** `/api/hero`
**Файл:** [app/api/hero/route.ts](app/api/hero/route.ts)

---

## 📋 КОЛЛЕКЦИЯ: PRODUCTS

**Тип:** Многоэлементная коллекция

**Количество элементов:** 7 продуктов

**Запрашиваемые поля из Directus:**
```
id, title, subtitle, description, ingredients, weight, product_photo.id, product_photo.filename_disk
```

**Передаваемые поля на фронт (8):**
- ✅ `id` - ID продукта
- ✅ `title` - Название продукта
- ✅ `subtitle` - Подзаголовок/сортировка
- ✅ `description` - Описание продукта
- ✅ `ingredients` - Ингредиенты
- ✅ `weight` - Вес (300g, 500g и т.д.)
- ✅ `image` - Объект с изображением (переименовано с `product_photo`)
  - Содержит: `id`, `filename_disk`, `url`
- ✅ `product_photo` - **Удаляется** при преобразовании (переименовано в `image`)

**API маршрут:** `/api/products`
**Файл:** [app/api/products/route.ts](app/api/products/route.ts)

---

## 📋 КОЛЛЕКЦИЯ: NEWS

**Тип:** Многоэлементная коллекция

**Количество элементов:** 6 новостей

**Запрашиваемые поля из Directus:**
```
id, slug, title, excerpt, date, content, news_photo.id, news_photo.filename_disk
```

**Передаваемые поля на фронт (7):**
- ✅ `id` - ID новости
- ✅ `slug` - URL-friendly идентификатор
- ✅ `title` - Заголовок новости
- ✅ `excerpt` - Краткое описание/превью
- ✅ `date` - Дата публикации
- ✅ `content` - Полное содержание статьи
- ✅ `news_photo` - Объект с изображением
  - Содержит: `id`, `filename_disk`, `url`

**API маршрут:** `/api/news`
**Файл:** [app/api/news/route.ts](app/api/news/route.ts)
**ISR кэш:** 60 секунд (быстрое обновление)

---

## 📋 КОЛЛЕКЦИЯ: MENU

**Тип:** Многоэлементная коллекция

**Запрашиваемые поля из Directus:**
```
id, label, href, order
```

**Передаваемые поля на фронт (4):**
- ✅ `id` - ID пункта меню
- ✅ `label` - Текст пункта меню
- ✅ `href` - Ссылка
- ✅ `order` - Порядок отображения

**API маршрут:** `/api/menu`
**Файл:** [app/api/menu/route.ts](app/api/menu/route.ts)

---

## 📋 КОЛЛЕКЦИЯ: SITE_SETTINGS

**Тип:** Singleton (1 запись)

**Запрашиваемые поля из Directus:**
```
ВСЕ поля (без фильтра)
```

**Передаваемые поля на фронт (22):**
- ✅ `id`
- ✅ `site_name` - Название сайта
- ✅ `site_url` - URL сайта
- ✅ `default_title` - Title по умолчанию для SEO
- ✅ `default_description` - Meta description по умолчанию
- ✅ `og_image` - OG изображение для соцсетей
- ✅ `og_image_width` - Ширина OG изображения
- ✅ `og_image_height` - Высота OG изображения
- ✅ `business_name` - Название компании
- ✅ `business_phone` - Телефон компании
- ✅ `business_email` - Email компании
- ✅ `business_address` - Адрес
- ✅ `business_city` - Город
- ✅ `business_postal_code` - Почтовый индекс
- ✅ `opening_hours` - Часы работы
- ✅ `price_range` - Ценовой диапазон
- ✅ `geo_latitude` - Широта для карты
- ✅ `geo_longitude` - Долгота для карты
- ✅ `social_instagram` - Ссылка на Instagram
- ✅ `social_vk` - Ссылка на VK
- ✅ `social_telegram` - Ссылка на Telegram

**API маршрут:** `/api/site-settings`
**Файл:** [app/api/site-settings/route.ts](app/api/site-settings/route.ts)

---

## 🔍 АНАЛИЗ: Какие поля ЗАПРАШИВАЮТСЯ vs СУЩЕСТВУЮТ в Directus

### HERO

**Запрашивается из Directus (через API маршрут):**
```javascript
fields=id,title,subtitle,button_text,button_link,hero_photo.id,hero_photo.filename_disk
```

**Используется в коде компонента:**
```
title, subtitle, image (переименовано от hero_photo), ctaText, ctaLink, imageAlt, backgroundImage
```

**Потенциально НЕДОСТАЮЩИЕ поля:**
- ❌ `image_alt` - запрашивается как `hero_image_alt` в коде, но НЕ запрашивается из Directus
- ❌ `background_image` - используется в коде, но НЕ запрашивается
- ❌ `hero_image_alt` - используется в коде, но НЕ запрашивается

### PRODUCTS

**Запрашивается из Directus:**
```javascript
fields=id,title,subtitle,description,ingredients,weight,product_photo.id,product_photo.filename_disk
```

**Все поля передаются на фронт ✅**

### NEWS

**Запрашивается из Directus:**
```javascript
fields=id,slug,title,excerpt,date,content,news_photo.id,news_photo.filename_disk
```

**Все поля передаются на фронт ✅**

### MENU

**Запрашивается из Directus:**
```javascript
fields=id,label,href,order
```

**Все поля передаются на фронт ✅**

### SITE_SETTINGS

**Запрашивается из Directus:**
```javascript
(БЕЗ фильтра fields - все поля)
```

**Все доступные поля передаются на фронт ✅**

---

## � ПЛАН МИГРАЦИИ ДАННЫХ

### **Статус компонентов:**

✅ **ПОДКЛЮЧЕНО К DIRECTUS:**
- Hero → `/api/hero`
- Products → `/api/products`
- News → `/api/news`
- Site Settings → `/api/site-settings`
- Menu → `/api/menu`

❌ **НЕ ПОДКЛЮЧЕНО (требует миграции):**
1. **WhyGlutenFreeSection** → нужен `/api/why-gluten-free`
   - Пытается fetch из `/api/why-gluten-free`
   - Имеет fallback hardcoded данные (4 benefits)
   
2. **FAQSection** → нужен `/api/faq`
   - Пытается fetch из Directus напрямую `/items/faq`
   - Имеет fallback hardcoded данные (8 вопросов-ответов)
   
3. **AboutSection** → нужен `/api/about`
   - Нужно проверить

4. **Footer** → нужен `/api/footer`
   - Нужно проверить

5. **Header** → может быть уже подключена или нужна миграция

### **Действия:**
1. Проверить и заполнить недостающие коллекции в Directus
2. Создать API маршруты для всех коллекций
3. Обновить компоненты для использования новых маршрутов

---

## �🚨 ПРОБЛЕМНЫЕ ЗОНЫ

### 1. HERO - Отсутствующие поля
Коллекция HERO использует поля, которые НЕ запрашиваются:
- `image_alt` / `hero_image_alt` - для атрибута alt изображения
- `background_image` - для фонового изображения
- `background_image_alt` - для alt фонового изображения

**Решение:** 
1. Либо добавить эти поля в запрос к Directus
2. Либо удалить эти поля из кода компонента, если они не используются

### 2. ISR Кэширование
- **HERO, PRODUCTS, SITE_SETTINGS:** 3600 сек (1 час) - долгий кэш
- **NEWS:** 60 сек (1 минута) - быстрый кэш (логично, т.к. новости часто обновляются)

**Рекомендация:** Рассмотреть уменьшение кэша для PRODUCTS с 3600 до 300-600 сек, если товары часто меняются.

### 3. HEADER коллекция
Используется в коде, но НЕ определена в этом анализе:
- **Файл:** [app/api/header/route.ts](app/api/header/route.ts)
- **Требует отдельного анализа**

---

## 📝 ВЫВОДЫ

✅ **Корректно настроены:**
- Products - все поля запрашиваются и передаются
- News - все поля запрашиваются и передаются  
- Menu - все поля запрашиваются и передаются
- Site Settings - все поля передаются

⚠️ **Требуют внимания:**
- **HERO** - есть неиспользуемые/отсутствующие поля (image_alt, background_image)

🔧 **Следующие шаги для обсуждения:**
1. Нужны ли в HERO поля `image_alt`, `background_image`?
2. Добавить ли их в запрос к Directus?
3. Есть ли другие скрытые поля в Directus, которые не используются?
4. Нужно ли проверить коллекцию HEADER?
