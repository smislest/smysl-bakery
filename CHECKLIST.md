# ✅ Чек-лист развертывания на сервере reg.ru

Используйте этот чек-лист для пошагового развертывания.

---

## 📋 Подготовка сервера

- [ ] Заказан VPS/VDS сервер на reg.ru
- [ ] Получены SSH данные (IP, логин, пароль)
- [ ] Успешное подключение к серверу: `ssh user@server-ip`
- [ ] Сервер на Ubuntu/Debian (проверка: `lsb_release -a`)

---

## 🐳 Установка Docker

- [ ] Скачан скрипт установки Docker: `curl -fsSL https://get.docker.com -o get-docker.sh`
- [ ] Установлен Docker: `sudo sh get-docker.sh`
- [ ] Пользователь добавлен в группу docker: `sudo usermod -aG docker $USER`
- [ ] Установлен Docker Compose: `sudo apt install docker-compose-plugin`
- [ ] Повторное подключение по SSH (для применения прав)
- [ ] Проверка версий:
  - [ ] `docker --version` показывает версию
  - [ ] `docker compose version` показывает версию

---

## 📦 Загрузка проекта

**Выберите один из вариантов:**

### Вариант A: Через Git

- [ ] Git установлен: `git --version`
- [ ] Репозиторий склонирован: `git clone <repo-url> smysl-bakery`
- [ ] Перешли в папку: `cd smysl-bakery`

### Вариант B: Через SFTP

- [ ] Подключились через WinSCP/FileZilla
- [ ] Создана папка `~/smysl-bakery` на сервере
- [ ] Загружены все файлы КРОМЕ:
  - [ ] ❌ node_modules/
  - [ ] ❌ .next/
  - [ ] ❌ .git/
  - [ ] ❌ *.log
- [ ] Все остальные файлы загружены:
  - [ ] ✅ Dockerfile
  - [ ] ✅ docker-compose.yml
  - [ ] ✅ .dockerignore
  - [ ] ✅ deploy.sh
  - [ ] ✅ package.json
  - [ ] ✅ next.config.js
  - [ ] ✅ app/ (вся папка)
  - [ ] ✅ public/ (вся папка)
  - [ ] ✅ lib/ (вся папка)
  - [ ] ✅ content/ (вся папка)

---

## ⚙️ Настройка переменных окружения

- [ ] Скопирован пример: `cp .env.production.example .env.production`
- [ ] Файл открыт для редактирования: `nano .env.production`
- [ ] Заполнены переменные:
  - [ ] `NEXT_PUBLIC_DIRECTUS_URL` - URL вашего Directus
  - [ ] `NEXT_PUBLIC_DIRECTUS_TOKEN` - токен доступа
  - [ ] `DIRECTUS_URL` - URL вашего Directus (тот же)
  - [ ] `DIRECTUS_TOKEN` - токен доступа (тот же)
  - [ ] `NODE_ENV=production`
- [ ] Файл сохранён (Ctrl+X, Y, Enter)
- [ ] Проверка: `cat .env.production` показывает ваши значения

---

## 🚀 Первый запуск

- [ ] Скрипт deploy.sh имеет права на выполнение: `chmod +x deploy.sh`
- [ ] Запущен скрипт развертывания: `./deploy.sh`
- [ ] Дождались завершения сборки (3-5 минут)
- [ ] Нет ошибок в выводе
- [ ] Проверка статуса: `docker compose ps` показывает "Up"

---

## ✔️ Проверка работы

- [ ] Просмотрены логи: `docker compose logs -f web`
- [ ] В логах видно: "▲ Next.js 16.1.1"
- [ ] В логах видно: "✓ Ready in XXms"
- [ ] Проверка через curl: `curl http://localhost:3000` возвращает HTML
- [ ] Открыт в браузере: `http://server-ip:3000`
- [ ] Сайт отображается корректно
- [ ] Изображения загружаются
- [ ] Навигация работает

---

## 🌐 Настройка домена (опционально)

- [ ] Домен куплен и настроен на IP сервера
- [ ] DNS A-запись указывает на сервер (проверка: `nslookup your-domain.ru`)
- [ ] Установлен Nginx: `sudo apt install nginx`
- [ ] Создан конфиг Nginx: `sudo nano /etc/nginx/sites-available/smysl-bakery`
- [ ] Конфиг скопирован из документации
- [ ] В конфиге указан ваш домен
- [ ] Создан symlink: `sudo ln -s /etc/nginx/sites-available/smysl-bakery /etc/nginx/sites-enabled/`
- [ ] Проверка конфига: `sudo nginx -t` - OK
- [ ] Перезапуск Nginx: `sudo systemctl restart nginx`
- [ ] Открыт в браузере: `http://your-domain.ru`
- [ ] Сайт открывается через домен

---

## 🔒 Настройка SSL (опционально)

- [ ] Установлен certbot: `sudo apt install certbot python3-certbot-nginx`
- [ ] Получен сертификат: `sudo certbot --nginx -d your-domain.ru -d www.your-domain.ru`
- [ ] Email указан для уведомлений
- [ ] Согласие с Terms of Service
- [ ] Сертификат успешно установлен
- [ ] Проверка автообновления: `sudo certbot renew --dry-run` - OK
- [ ] Открыт в браузере: `https://your-domain.ru`
- [ ] Зелёный замочек SSL в браузере
- [ ] HTTP автоматически перенаправляется на HTTPS

---

## 🔧 Настройка автозапуска

- [ ] Docker включен в автозагрузку: `sudo systemctl enable docker`
- [ ] Проверка: `sudo systemctl is-enabled docker` → "enabled"
- [ ] Nginx включен в автозагрузку: `sudo systemctl enable nginx`
- [ ] Проверка: `sudo systemctl is-enabled nginx` → "enabled"

---

## 🛡️ Безопасность (рекомендуется)

- [ ] Настроен firewall:
  - [ ] `sudo ufw allow 22/tcp` (SSH)
  - [ ] `sudo ufw allow 80/tcp` (HTTP)
  - [ ] `sudo ufw allow 443/tcp` (HTTPS)
  - [ ] `sudo ufw deny 3000/tcp` (закрыть прямой доступ)
  - [ ] `sudo ufw enable`
- [ ] Проверка: `sudo ufw status`
- [ ] SSH ключи настроены (вместо пароля)
- [ ] Пароль root изменён
- [ ] Создан отдельный пользователь (не root)

---

## 📊 Настройка мониторинга (опционально)

- [ ] Создан скрипт health-check (из MONITORING.md)
- [ ] Скрипт добавлен в crontab
- [ ] Создан скрипт backup (из DEPLOYMENT.md)
- [ ] Backup добавлен в crontab
- [ ] Настроены уведомления (Telegram/Email)

---

## 📝 Финальная проверка

- [ ] Сайт открывается по домену
- [ ] SSL работает (https://)
- [ ] Все страницы загружаются
- [ ] Изображения отображаются
- [ ] Контент из Directus загружается
- [ ] Адаптивный дизайн работает (проверка на мобильном)
- [ ] Формы работают (если есть)
- [ ] Логи не показывают критических ошибок
- [ ] Контейнер автоматически перезапускается после `sudo reboot`

---

## 📖 Документация сохранена

- [ ] Записаны данные доступа:
  - [ ] IP сервера
  - [ ] SSH логин/пароль или ключ
  - [ ] Путь к проекту на сервере
  - [ ] Домен
  - [ ] Directus URL и токен
- [ ] Сохранён файл .env.production (локально, не в Git!)
- [ ] Известны команды управления:
  - [ ] Просмотр логов: `docker compose logs -f web`
  - [ ] Перезапуск: `docker compose restart`
  - [ ] Обновление: `git pull && ./deploy.sh`

---

## 🎉 Развертывание завершено!

Если все пункты отмечены ✅, поздравляем - ваш сайт успешно развернут!

### Полезные команды для повседневного использования:

```bash
# Подключение к серверу
ssh user@server-ip

# Переход в папку проекта
cd ~/smysl-bakery

# Просмотр статуса
docker compose ps

# Просмотр логов
docker compose logs -f web

# Перезапуск
docker compose restart

# Обновление кода
git pull origin main
./deploy.sh

# Проверка использования ресурсов
docker stats
```

---

## 🆘 Что делать при проблемах

1. **Проверьте логи**: `docker compose logs web`
2. **Проверьте статус**: `docker compose ps`
3. **Перезапустите**: `docker compose restart`
4. **Читайте документацию**: [DEPLOYMENT.md](DEPLOYMENT.md), [MONITORING.md](MONITORING.md)
5. **Обратитесь в поддержку**: support@reg.ru

---

**Успешной работы! 🚀**
